---
layout: page
title: More on my PhD
---
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>kPCA with Gaussian kernel</title>
    <style>
        iframe {
            border: none; /* Remove scrolling bars */
            display: block; /* Centering helper if needed */
            width: 50%;
	    height: 50%;
        }
    </style>
</head>
<body>
    <iframe id="myIframe" src="kpca_rbf_3d_global_elements.html"></iframe>

    <script>
        const iframe = document.getElementById('myIframe');

        window.addEventListener('message', (event) => {
            // Optional: Validate the event.origin if you know where the iframe is hosted
            const { width, height } = event.data;
            iframe.style.width = width + 'px';
            iframe.style.height = height + 'px';
        });
    </script>
</body>
